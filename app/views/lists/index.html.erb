<div class="banner" style="background-image: url(https://images7.alphacoders.com/769/769813.jpg";>
  <div class="container">
    <h1>All your movies in one place!</h1>
    <p>Now all you have to do is pop some popcorn and have a good session</p>
    <p><%= link_to "Add new movie", new_list_path, class:"btn btn-ghost " %> </p>
  </div>

</div>
    <div class="box-flex mb-4">
      <% @lists.each do |list| %>
        <div >
          <img src="<%= list.images %>", class: "img-list" >
          <p><%= link_to list.name, list_path(list), class:"btn btn-ghost" %> </p>
        </div>
      <% end %>
    </div>

